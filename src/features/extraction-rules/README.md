# extraction-rules feature

This feature owns rule editing UI, parser patterns, and the extraction engine. Keep parsing, validation, and rule persistence here.

Tests should be colocated with the component files (e.g., `components/Rules/Rules.test.tsx`).
