# document-processing feature

This feature owns the document-level composition: Viewer + Workspace + document services. Keep orchestration and page-level flows here. Tests should be colocated with components (e.g., `components/Viewer/Viewer.test.tsx`).
