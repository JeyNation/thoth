'use client';

import React from 'react';
import { Box } from '@mui/material';

const Rules: React.FC = () => {
    return (
        <Box sx={{ p: 2 }}>
            {/* Rules component - to be implemented */}
        </Box>
    );
};

export default Rules;
